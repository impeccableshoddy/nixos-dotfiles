# Vendored font files go here (see docs/DEPENDENCIES.md §Vendored assets):
#   DepartureMono-Regular.otf  — https://departuremono.com  (SIL OFL 1.1, ~80KB)
#   CommitMono-Regular.otf     — https://commitmono.com     (SIL OFL 1.1, ~110KB)
#
# Loaded at startup via `include_bytes!` in `render::theme`.
# Not committed to the repo yet — vendoring happens in a future commit
# `dodona(render): vendor Departure Mono + Commit Mono fonts`.
