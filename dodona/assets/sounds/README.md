# Generated WAV files go here (see docs/SOUND_DESIGN.md §Implementation):
#   ding.wav, blip.wav, fail.wav, warn.wav, alarm.wav, alert.wav
#
# Generated at build time by build.rs using a small Rust synth.
# .gitignore'd — they're regenerated on every build, ~5KB each.
